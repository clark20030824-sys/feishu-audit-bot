# 飞书 @审核机器人 —— 上线部署清单

目标：群里 `@机器人` + 发「客户要求」「设计方案」两份文件 → 机器人自动出审核报告并 `@负责人`。
（webhook 群机器人做不到这一步，必须走「自建应用 + 订阅消息事件」，本清单即为此而写。）

---

## 第 1 步 · 飞书开放平台建自建应用
1. 打开 https://open.feishu.cn/  →  企业自建应用  →  创建应用
2. 记下 **App ID** 和 **App Secret**（填入项目 `.env` 的 `FEISHU_APP_ID` / `FEISHU_APP_SECRET`）
3. 应用名称建议：`包装审核机器人`

## 第 2 步 · 开权限（权限管理 → 权限范围）
勾选并**申请发布**：
- `im:message` （接收与读取消息）
- `im:message:send_as_bot` （以机器人身份发消息）
- `im:resource:attach:download` （下载附件，读图纸/客户要求）
- `contact:user.id:readonly` （解析 @ 了谁，可选）

## 第 3 步 · 订阅事件（事件订阅）
- 开启 **接收消息** 事件：`im.message.receive_v1`
- Request URL 填：`https://<你的公网域名或 ngrok 地址>/feishu/event`
  - 本地联调：`node index.js` 起在 3000，用 `start-tunnel.bat` 开 ngrok，把 `https://xxxx.ngrok-free.app/feishu/event` 填进来
  - 保存时飞书会发 `url_verification` 挑战，本服务自动回 `challenge` 通过
- 事件签名：事件订阅页的「加密 key」就是你的 App Secret，服务用它对 `x-lark-signature` 做 HmacSHA256 校验（代码已内置）

## 第 4 步 · 把机器人加进群 + 发布应用
1. 回到飞书客户端 → 你的审核群 → 群设置 → 群机器人 → 添加机器人 → 搜你刚建的自建应用 → 加入
2. 开放平台 → **发布版本** → 提交审核/发布（企业内应用通常管理员审批即可）
3. 发布后机器人才能在该群里收发消息

## 第 5 步 · 本地/服务器启动
```bash
cp .env.example .env   # 或已在 .env 填好
npm install
npm start              # 或 node index.js
```
- 本地无公网：用 `start-tunnel.bat` 一键 `node index.js` + ngrok
- 生产：把服务部署到任意公网服务器（端口 3000），nginx 反代到 `/feishu/event`

## 第 6 步 · 验证
1. 群里 `@包装审核机器人` 并发一张「客户要求」→ 应回“已收到 1 份素材，请再发送另一份”
2. 再发一张「设计方案」→ 凑齐 2 份 → 自动回“正在生成…” → 出审核报告卡片并 `@负责人`
3. 或发完 1 份后直接回复“生成”→ 立即出报告

---

## 常见问题
- **@了没反应**：①机器人没加进群或未发布；②Request URL 没通过验证；③消息没真正 @ 到机器人（确认 @ 的是本应用机器人）。
- **收不到附件**：检查 `im:resource:attach:download` 权限是否已申请并发布。
- **报告乱码/太短**：模型把 HTML 转文本时丢内容，可在 `auditPrompt.js` / `toPlainText` 里放宽长度上限（当前 12000 字符）。
- **想换模型口径**：改 `.env` 的 `LLM_API_BASE` / `LLM_API_KEY` / `LLM_MODEL`；接 UPP/antpkg 只需改 base。
- **固定 @ 某人**：在 `.env` 填 `AUDITOR_OPEN_ID=ou_xxx`；留空则默认 @ 触发人。
